# Script boundary

Existing scripts are retained for v1 provenance. New ICLR-v2 entry points must
resolve paths from the repository root and call the `ptl` package rather than
embedding absolute machine-specific paths.
