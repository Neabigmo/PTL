# Figure 3 Decay Heatmap

- Message: Model fidelity and rank shifts change across random and context-stress split families.
- Input table: results/tables/transfer_decay_summary.csv
- Visual encoding: Cell color encodes mean non-control cosine; annotations report cosine and rank change versus the random anchor.
- Limitation: Stress-retention ratios can be unstable for small random anchors, so rank shifts are shown as directional context rather than causal effects.
