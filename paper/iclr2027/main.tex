\documentclass{article}
\usepackage{../../third_party/iclr2027/iclr2027_conference,times}
\input{../../third_party/iclr2027/math_commands.tex}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{multirow}

\title{Reliability Transportability Has a Measurement and Decision Boundary in Single-Cell Perturbation Prediction}
\author{Anonymous Authors}

\newcommand{\ptl}{\textsc{PTL}}
\newcommand{\uq}{\textsc{UQ}}
\newcommand{\gears}{\textsc{GEARS}}
\newcommand{\slim}{\textsc{SLIM}}

\begin{document}
\maketitle

\begin{abstract}
Confidence scores from single-cell perturbation predictors are often used to
rank experiments, although the identity of easy and difficult perturbations
may change after a biological shift. We ask when such a reliability reordering
is identifiable and when it changes a decision. In a source-frozen Frangieh
ladder, one predictor is evaluated unchanged across control, co-culture and
IFN$\gamma$ outcomes using a tie-aware pairwise ordering estimand, matched
raw-cell measurement floors, three metrics, 30 seeds and 2,000 synchronized
bootstrap draws. The full-size measurement-identifiable excess is 0.0255--0.1140
across the three metrics, while the joint-floor excess ranges from $-0.0512$ to
0.0364, so the evidence is metric-dependent rather than universal. At a fixed
10\% decision budget, the prespecified full-depth link between the identifiable
excess and normalized regret contains 18 directed transfer--metric points and
is descriptive (Spearman $\rho=0.80$). An outcome-blind Nadig HepG2/Jurkat
replication and a source-only held-out analysis delimit the result: biological
context can reshape reliability ordering, but measurement depth and prospective
feature availability determine what can be known before a new experiment.
\end{abstract}

\section{Introduction}
Single-cell perturbation models turn large screens into predictions that can
prioritize experiments over a combinatorial intervention space. Their value
therefore depends not only on average fidelity, but also on whether a researcher
can decide which predictions to trust before spending experimental budget. An
uncertainty score learned in one context need not preserve the same meaning in
another context. This question is related to, but distinct from, response
prediction itself.

Accuracy alone does not settle the decision problem. Deep perturbation
predictors do not yet reliably outperform simple linear baselines, while
Systema and scPertEval show that reference construction, split design and
metric choice can change conclusions~\citep{ahlmann2025simple,wong2025simple,systema2025,scperteval2026}.
The ordering of trusted versus risky predictions is therefore an object in its
own right.

Calibration and selective prediction ask whether a score tracks error in a
fixed task, while shift studies ask whether representations transport
\citep{guo2017calibration,geifman2017selective,ovadia2019uncertainty,liang2024selective}.
Neither alone establishes that the identity of easy and hard perturbations
survives a biological change~\citep{jalaldoust2024transport}. We study this
narrow operational question with source-frozen predictions and target-outcome-
blind selection.

Our central claim is an identifiability boundary: a visible cross-context
ordering difference becomes a scientific transport result only after the same
ordering statistic is compared with matched experimental and model floors.
Figure~\ref{fig:framework} summarizes the resulting path from biological shift
to ordering, measurement and decision.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{../../results/figures/reliability_transportability/reliability_transportability_fig1_graphical_abstract.pdf}
  \caption{\textbf{From biological shift to a decision boundary.} A single
  source-frozen predictor is evaluated across three outcomes; crossing ranking
  trajectories represent observed reordering, which is decomposed into a
  measurement-identifiable component. Depth, top-$k$ retention and regret are
  linked schematically. The panel is conceptual and does not add an estimand.}
  \label{fig:framework}
\end{figure*}

\section{From observed to identifiable reliability reordering}
For a perturbation pair $c=(p,q)$, context $e$, and measurement replicate $r$,
let $R_{p,e}^{(r)}$ be continuous risk from a fixed predictor and define
\begin{equation}
 Z_{c,e}^{(r)}=\operatorname{sign}\!\left(R_{p,e}^{(r)}-R_{q,e}^{(r)}\right)\in\{-1,0,+1\}.
\end{equation}
Ties are retained explicitly through
$\pi_{c,e}=[\Pr(Z=-1),\Pr(Z=0),\Pr(Z=+1)]$. For two contexts,
\begin{equation}
 D_{\rm cross}=\mathbb{E}_{c}[1-\pi_{c,e_1}^{\mathsf T}\pi_{c,e_2}],\qquad
 D_{\rm within,e}=\mathbb{E}_{c}[1-\|\pi_{c,e}\|_2^2].
\end{equation}
The population measurement-identifiable component is
\begin{equation}
 D_{\rm meas\text{-}ID}=D_{\rm cross}-
 \frac{D_{\rm within,e_1}+D_{\rm within,e_2}}{2}
 =\frac{1}{2}\mathbb{E}_{c}\|\pi_{c,e_1}-\pi_{c,e_2}\|_2^2\geq0.
 \label{eq:ordering_identity}
\end{equation}
This equality is an identity, not a causal statement. For finite replicates we
use the leave-one-replicate-out U-statistic
\begin{equation}
 \widehat D_{\rm within}^{U}=1-
 \frac{n_{-}(n_{-}-1)+n_{0}(n_{0}-1)+n_{+}(n_{+}-1)}{n(n-1)}.
 \label{eq:u_floor}
\end{equation}
The joint operational floor is intentionally conservative,
$D_{\rm joint\text{-}ID}=D_{\rm cross}-\max(D_{\rm joint,e_1},D_{\rm joint,e_2})$,
and is not assumed to add the measurement and model floors. Continuous rank
displacement is retained as a secondary magnitude diagnostic. Stable ordering
uses a tie-aware 15/15 seed cross-fit, a 95\% Beta interval and support of at
least eight strict observations; it does not select the headline metric.

\section{Biological context reshapes reliability ordering}
The Frangieh surface contains 243 shared perturbation labels across control,
co-culture and IFN$\gamma$. For every global five-fold label assignment, the
source predictor is fit once and its prediction vector is held fixed while the
target raw-cell outcome changes. We use delta cosine, Systema centroid accuracy
and absolute-effect rank agreement, with 30 independent seeds and matched
full-size raw-cell pseudo-replicates. The primary evidence is organized by
source context and by the biological contrast being transferred, rather than
by a source-to-single-target forest.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{../../results/figures/reliability_transportability/reliability_transportability_fig2_transport_atlas.pdf}
  \caption{\textbf{Reliability Transport Atlas.} Heatmaps show the Frangieh
  measurement-identifiable excess for each source context and each left/right
  biological contrast; dots indicate a positive 90\% lower interval and rings
  indicate a positive joint-floor lower interval. Solid and dashed borders
  retain Tier 1 and Tier 2 evidence, respectively. The Nadig HepG2/Jurkat
  directions are shown separately as an independent replication.}
  \label{fig:atlas}
\end{figure*}

\begin{table}[t]
\centering
\caption{Full-size Frangieh primary ordering estimates. Intervals are 90\%
macro-bootstrap intervals for the stated estimand.}
\label{tab:primary}
\begin{tabular}{lrrr}
\toprule
Metric & $D_{\rm cross}$ & $D_{\rm meas\text{-}ID}$ & $D_{\rm joint\text{-}ID}$ \\
\midrule
Delta cosine & 0.3153 & 0.1140 [0.0816, 0.1500] & 0.0364 [-0.0025, 0.0761] \\
Systema centroid & 0.0969 & 0.0255 [0.0153, 0.0364] & 0.0220 [0.0117, 0.0331] \\
Absolute-effect rank & 0.2786 & 0.0574 [0.0284, 0.0850] & -0.0512 [-0.0888, -0.0145] \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:atlas} shows why the estimand must retain both the source and
the left/right contrast. The three metrics do not share a single transport
surface: the largest measurement-identifiable excess is observed for delta
cosine, while the joint-floor stress test can remain negative for
absolute-effect rank. These signs are interpreted only with their declared
floors and intervals; a negative joint-floor result is not evidence that the
underlying ordering distributions are identical.

\section{Measurement depth defines the identifiability boundary}
We next hold the perturbation universe fixed at the highest matched budget and
resample 10, 20, 40, 80 and 160 cells per environment--perturbation
pseudoreplicate, together with the matched full-depth endpoint. For each
source-by-contrast cell, the depth ratio is
$R(n)=D_{\rm meas\text{-}ID}(n)/D_{\rm meas\text{-}ID}(\mathrm{full})$.
The first budget within ten percent of the full value defines a resolution
summary; the first budget with a positive 90\% lower interval defines
detectability. If no budget meets a rule, the threshold remains unavailable.
This makes the measurement boundary an explicit property of the cell and
metric, not a universal cutoff.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{../../results/figures/reliability_transportability/reliability_transportability_fig3_measurement_boundary.pdf}
  \caption{\textbf{Measurement depth sets resolution.} Each heatmap reports
  $R(n)$ for one metric across the nine source-context/contrast cells; raw
  values above one are retained and unavailable cells remain gray. The lower
  curves summarize group-level interval endpoints and do not impose a common
  depth threshold.}
  \label{fig:boundary}
\end{figure*}

\section{Identifiable reordering changes experimental decisions}
At decision budgets $k/n\in\{0.05,0.10,0.20,0.50\}$, each source context selects the
lowest source risk and the target evaluates the selected set. We report target
top-$k$ retention and normalized regret. The point estimate is the frozen
30-seed mean, while the displayed uncertainty is the canonical 2,000-draw
perturbation-label-by-measurement-seed interval. This decision layer is kept
separate from the global ordering estimand.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{../../results/figures/reliability_transportability/reliability_transportability_fig4_decision_consequence.pdf}
  \caption{\textbf{Decision consequence at fixed budgets.} The source ranking
  is evaluated against target utility, retention and excess regret are traced
  over the four fixed budgets, and the primary association links full-depth
  $D_{\rm meas\text{-}ID}$ to normalized regret at 10\%. The association uses
  18 transfer--metric points and is descriptive rather than causal.}
  \label{fig:decision}
\end{figure*}

The prespecified full-depth, 10\% link contains six directed biological
transfers and the three metrics. Its Spearman association is $\rho=0.80$;
metric-specific associations are not treated as headline tests because each
contains only six transfers. The panel is retained even if a future replication
weakens the association: the scientific question is whether identifiable
ordering loss has a measurable decision consequence, not whether a single
summary correlation is universal.

\section{What drives transport failure, and can we know it beforehand?}
The failure anatomy reuses the existing per-perturbation identifiable burden,
the matched response-program displacement and the existing response-magnitude
diagnostic. These quantities are explanatory and target-informed. They are
therefore not allowed into the prospective predictor. The prospective analysis
uses only source-side prediction geometry and model-member uncertainty, with
leave-label-out evaluation and target outcomes used only after prediction.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{../../results/figures/reliability_transportability/reliability_transportability_fig5_failure_anatomy.pdf}
  \caption{\textbf{Failure anatomy and prospective predictability.} Tie-aware
  source-to-target matrices report stable inversion fractions and retain
  unresolved pairs separately. Response displacement versus reordering burden
  is post-hoc explanatory, with marker size encoding model disagreement. The
  final panel evaluates held-out target outcomes from source-only features;
  target-informed explanatory quantities never enter that prospective fit.}
  \label{fig:failure}
\end{figure*}

The source-only result is intentionally bounded. A model can identify a
useful source-side warning signal without proving a biological mechanism, and
the absence of a signal is not evidence that transport failure has no cause.
The relevant interpretation is operational: before a new target outcome is
observed, only source-side descriptors are admissible.

\section{Deployment probe and feasibility boundary}
PTL is evaluated only as a deployment-side probe. Its feature surface keeps
predictions, outcome-free descriptors, observed outcomes and split assignments
separate. A source-selection gate is accepted only under a fixed
validation-only acceptance criterion and falls back to an uncertainty-only
score otherwise; this is a heuristic rather than a safety guarantee. The
outcome-blind analysis reports the gate and its negative-transfer cases in the
supplement, without allowing them to alter the five primary figures.

\section{Bounded robustness and limitations}
The outcome-blind registry scanned 33 candidate same-study pairs before risk or
inversion evaluation. Nadig HepG2--Jurkat has supported independent-context
provenance and is the supported independent pair;
its result is a bounded Tier 1 replication and is not pooled with the Frangieh
depth or decision curves. The original split-half and $n\geq40$ analyses remain
sensitivity analyses under the same protocol.

The broader benchmark contains 1,603 condition-level instances and 8,229
scored genes. The Claim Lock roster is matching mean, an Ahlmann--Eltze-style
bilinear model and \slim{} with a public STRING embedding, all using fixed
biological-instance splits and three model members. Modern-model scope remains
unchanged: \gears{} has an external CUDA contract but no frozen Frangieh or
Nadig vector, TxPert is limited to its cached K562 contract, State has no
promoted same-contract vector, and scGPT stops at the Windows torchtext ABI.
These are feasibility blockers, not scientific transport results.

% Artifact-compatibility lock for the legacy supplement renderer:
% \caption{\textbf{Replication and model-scope boundary.} Outcome-blind eligibility precedes risk evaluation; metric-dependent evidence is not a universal reordering claim.}
% \label{fig:replication}

\section{Discussion}
The evidence supports a limitation-centered interpretation. Source-frozen
orderings visibly change across matched biological settings, but the amount
that can be attributed beyond measurement and model floors depends on the
metric. Measurement depth determines which cells of the atlas are resolvable;
the decision analysis shows how an unresolved ordering can affect top-$k$
retention and regret. Prospective prediction is a separate question with a
strict source-only feature boundary.

This design also clarifies how to read negative results. A negative joint-floor
excess can arise from conservative asymmetric floors and does not prove no
biological difference. It should trigger checks of measurement depth, model
member disagreement, metric geometry and implementation identity before a
mechanism is assigned. The Nadig registry protects against choosing a favorable
pair after observing outcomes, but one replication cannot establish universal
transportability.

\section{Reproducibility}
All paper values are regenerated from fixed splits, native predictor spaces,
source-frozen vectors, matched raw-cell resampling, tie-aware ordering rows and
the declared bootstrap summaries. The supplement gives the complete estimator
definitions, provenance map, source tables, feasibility boundaries and
validation commands. The five figure source tables are released alongside the
editable vector figures.

\clearpage
\bibliographystyle{../../third_party/iclr2027/iclr2027_conference}
\bibliography{../../manuscript/references}
\end{document}
